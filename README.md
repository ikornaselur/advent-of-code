# Advent of code
A repo for advent of code challenges

## Running

The project uses `poetry` for dependency management and provides a script target to run all the solutions with

```
poetry run year 2020
```

Use `poetry run year --help` for more options
